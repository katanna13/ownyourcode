"""Persisted Existing Repository learning workspaces.

This module is deliberately separate from the public, browser-session preview
workflow.  It owns only authenticated project snapshots, immutable workspace
definitions, submitted attempts, and the small mutable resume projection.
"""
